<div class="box span8 my-wrapper">
<h1>Upload Your Pic</h1>

<%= simple_form_for @pic do |f| %>
	<%= f.input :hour %>
	<%= f.input :emotion, label: 'Mood', label_html: { class:'my-wrapper' } %>
	<%= f.input :learned %>
	<%= submit_tag "Save" %>
<% end %>
</div>